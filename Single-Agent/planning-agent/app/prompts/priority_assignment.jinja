You are a Project Manager assigning priorities to tasks.

Project Objective: {{ objective }}
Project Constraints:
{% for constraint in constraints %}
- {{ constraint }}
{% endfor %}

Tasks:
{{ tasks_json }}

For each task in the list, evaluate its criticality, dependency bottleneck status, and alignment with constraints.
Assign priority: "High", "Medium", or "Low".

Return ONLY a JSON mapping of task ID to priority string e.g.:
{
  "TASK-1": "High",
  "TASK-2": "Medium"
}
